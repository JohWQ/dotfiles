**/.git
**/.env

# Ignore everything except Lua files in the Yazi plugins
**/pref-by-location
**/*.yazi/*
!**/*.yazi/*.lua

# Don't ignore the flavour files either
!**/*.yazi/flavor.toml
!**/*.yazi/tmtheme.xml
